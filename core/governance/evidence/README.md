# Evidence Ledger

Audit trail for data operations. Contains metadata only (run records, manifests, checksums) — actual data lives under `artifacts/data/`.

- `downloads/` — records of what was fetched and when
- `processing/` — records of screening, deduplication, verification runs
- `releases/` — records of public dataset releases
