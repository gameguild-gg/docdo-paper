# Canonical Schemas

Internal canonical schema definitions (template).

- Keep schema files versioned.
- Link published schemas from [governance/registry/schemas/public/](governance/registry/schemas/public/).
